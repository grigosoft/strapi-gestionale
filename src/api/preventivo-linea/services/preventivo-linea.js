'use strict';

/**
 * preventivo-linea service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::preventivo-linea.preventivo-linea',
({strapi})=>({
    /**
     * 
     * @param {*} params 
     */
    async inizializza(params) {

    }
}))
