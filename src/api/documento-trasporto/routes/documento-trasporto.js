'use strict';

/**
 * documento-trasporto router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::documento-trasporto.documento-trasporto');
